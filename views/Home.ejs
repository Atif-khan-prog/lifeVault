<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>LifeVault - Your Personal Life Manager</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      background-color: #2c2f33;
      font-family: 'Arial', sans-serif;
      color: #ffffff;
      overflow-x: hidden; /* Prevent horizontal scroll */
    }

    /* Header */
    .header {
      background-color: #36393f;
      padding: 1rem 1.5rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      position: fixed;
      width: 100%;
      top: 0;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);
      box-sizing: border-box;
      z-index: 1000;
    }

    .logo {
      font-size: 1.5rem;
      font-weight: 700;
      color: #7289da;
    }

    nav ul {
      list-style: none;
      margin: 0;
      padding: 0;
      display: flex;
      gap: 1.5rem;
    }

    nav a {
      color: #b9bbbe;
      text-decoration: none;
      font-size: 1rem;
      transition: color 0.3s ease;
    }

    nav a:hover,
    nav a.active {
      color: #7289da;
    }

    /* Main Content */
    main {
      margin-top: 70px; /* Adjusted for header */
      padding: 1.5rem;
    }

    .hero {
      text-align: center;
      padding: 2rem 1rem;
    }

    .hero h1 {
      font-size: clamp(1.8rem, 5vw, 2.2rem); /* Responsive font size */
      margin-bottom: 1rem;
      color: #ffffff;
    }

    .hero p {
      font-size: clamp(0.9rem, 4vw, 1.1rem);
      color: #b9bbbe;
      margin-bottom: 1.5rem;
      max-width: 90%;
      margin-left: auto;
      margin-right: auto;
    }

    .cta-button {
      display: inline-block;
      padding: 0.8rem 1.8rem;
      background-color: #7289da;
      color: #ffffff;
      text-decoration: none;
      border-radius: 5px;
      font-weight: 500;
      font-size: 1rem;
      transition: background-color 0.3s ease;
      min-width: 120px; /* Ensure tap-friendly size */
      text-align: center;
    }

    .cta-button:hover {
      background-color: #677bc4;
    }

    .features {
      padding: 2rem 1rem;
      text-align: center;
    }

    .features h2 {
      font-size: clamp(1.5rem, 4vw, 1.8rem);
      margin-bottom: 1.5rem;
      color: #ffffff;
    }

    .feature-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 1.5rem;
    }

    .feature-card {
      background-color: #40444b;
      padding: 1.2rem;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
      transition: transform 0.3s ease;
    }

    .feature-card:hover {
      transform: translateY(-5px);
    }

    .feature-card h3 {
      font-size: clamp(1.2rem, 3vw, 1.4rem);
      margin-bottom: 0.8rem;
      color: #7289da;
    }

    .feature-card p {
      font-size: clamp(0.85rem, 2.5vw, 0.95rem);
      color: #b9bbbe;
    }

    /* Footer */
    .footer {
      background-color: #36393f;
      padding: 1rem;
      text-align: center;
      color: #b9bbbe;
      font-size: 0.85rem;
    }

    .footer ul {
      list-style: none;
      margin: 0.5rem 0 0;
      padding: 0;
      display: flex;
      justify-content: center;
      gap: 1.2rem;
    }

    .footer a {
      color: #7289da;
      text-decoration: none;
      transition: color 0.3s ease;
    }

    .footer a:hover {
      color: #677bc4;
    }

    /* Mobile Menu (Hamburger) */
    .nav-toggle {
      display: none;
      font-size: 1.5rem;
      background: none;
      border: none;
      color: #b9bbbe;
      cursor: pointer;
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      main {
        padding: 1rem;
      }

      .hero {
        padding: 1.5rem 0.5rem;
      }

      .features {
        padding: 1.5rem 0.5rem;
      }

      .feature-grid {
        grid-template-columns: 1fr; /* Single column on smaller screens */
        gap: 1rem;
      }
    }

    @media (max-width: 480px) {
      .header {
        padding: 0.8rem 1rem;
        flex-wrap: wrap;
      }

      .nav-toggle {
        display: block;
      }

      nav {
        width: 100%;
        max-height: 0;
        overflow: hidden;
        transition: max-height 0.3s ease;
      }

      nav.open {
        max-height: 300px; /* Adjust based on menu height */
      }

      nav ul {
        flex-direction: column;
        align-items: center;
        padding: 1rem 0;
      }

      nav a {
        font-size: 1.1rem;
        padding: 0.5rem 0;
        display: block;
      }

      .cta-button {
        padding: 0.7rem 1.5rem;
        font-size: 0.95rem;
      }

      main {
        margin-top: 60px; /* Adjust for smaller header */
      }
    }
  </style>
</head>
<body>
  <!-- Header -->
  <header class="header">
    <div class="logo">LifeVault</div>
    <button class="nav-toggle" aria-label="Toggle navigation">☰</button>
    <nav>
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/login">Login</a></li>
        <li><a href="/signup">Sign Up</a></li>
        <li><a href="/about">About</a></li>
        <li><a href="/contact">Contact</a></li>
      </ul>
    </nav>
  </header>

  <!-- Main Content -->
  <main>
    <section class="hero">
      <h1>Welcome to LifeVault</h1>
      <p>Your all-in-one solution to manage your daily diary, memories, and finances.</p>
      <a href="/login" class="cta-button">Get Started</a>
    </section>

    <section class="features">
      <h2>Explore LifeVault</h2>
      <div class="feature-grid">
        <div class="feature-card">
          <h3>Daily Diary</h3>
          <p>Record your thoughts, experiences, and daily moments with ease.</p>
        </div>
        <div class="feature-card">
          <h3>Memory</h3>
          <p>Store and cherish your precious memories in a secure, organized way.</p>
        </div>
        <div class="feature-card">
          <h3>Financial Management</h3>
          <p>Track your income, expenses, and budgets to stay financially secure.</p>
        </div>
      </div>
    </section>
  </main>

  <!-- Footer -->
  <footer class="footer">
    <p>© 2025 LifeVault. All rights reserved.</p>
    <ul>
      <li><a href="/about">About</a></li>
      <li><a href="/contact">Contact</a></li>
    </ul>
  </footer>

  <script>
    // Hamburger menu toggle
    const navToggle = document.querySelector('.nav-toggle');
    const nav = document.querySelector('nav');

    navToggle.addEventListener('click', () => {
      nav.classList.toggle('open');
      navToggle.textContent = nav.classList.contains('open') ? '✕' : '☰';
    });
  </script>
</body>
</html>